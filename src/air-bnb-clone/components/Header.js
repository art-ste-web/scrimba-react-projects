import logo from '../img/air-b-logo.png';
function Header() {
    return(
        <header className="header">
            <img src={logo} alt="logo"/>
        </header>
    )
}

export default Header;